# frontend/src/routes

TODO
- [ ] AppRoutes.jsx — defines all routes, wraps protected routes in <ProtectedRoute>
- [ ] Route list should mirror backend/src/routes/*.routes.js 1:1 in naming so it's easy to trace
      frontend page -> backend endpoint
