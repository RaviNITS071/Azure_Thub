# frontend/src/components — shared/reusable UI

TODO
- [ ] Layout/ — AppShell, Sidebar, Topbar
- [ ] ui/ — Button, Input, Select, Modal, Table, Badge, Spinner (plain Tailwind components; add shadcn/ui
      later only if the team wants it, not required at this scale)
- [ ] TenderCard.jsx, MatchScoreBadge.jsx, DocumentUploader.jsx (drives the presign/confirm upload flow)
- [ ] ProtectedRoute.jsx — redirects to /login if AuthContext has no user
